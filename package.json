{
  "name": "hong-design",
  "version": "1.0.0",
  "private": true,
  "description": "Portfolio site for Hong Design.",
  "scripts": {
    "build": "node scripts/generate-project-files.js && node scripts/prerender.js && node scripts/generate-sitemap.js",
    "prerender": "node scripts/prerender.js"
  },
  "engines": {
    "node": "22.x"
  }
}
